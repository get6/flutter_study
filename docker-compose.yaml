services:
  frontend:
    build:
      context: .
    ports:
      - "8080:8080"
    volumes:
      - .:/app
      # - flutter-sdk:/home/ittae/flutter

# volumes:
#   flutter-sdk:
#     name: flutter-sdk
#     driver: local
#     driver_opts:
#       type: none
#       o: bind
#       device: ${HOME}/goinfre/flutter
